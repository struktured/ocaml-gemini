(jbuild_version 1)

(library (
  (name gemini_test)
  (flags (
    :standard
    -open Core
    -open Async
  ))
  (library_flags (-linkall))
  (libraries (gemini ppx_inline_test.runtime-lib))
  (preprocess (pps (ppx_jane ppx_driver.runner)))
))
